# Arbor Mobile App (Frontend)

This folder is reserved for the 100% mobile client.

- If using Flutter: place the Flutter project here.
- If using React Native: place that project here.

**Important:** do not commit `.env*` files or build outputs.

## Current status
- The previous folder only contained a generated Next.js `.next/` build output. That is not source code.
- We will rebuild the real mobile app source here.
