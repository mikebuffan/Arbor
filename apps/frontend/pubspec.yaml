name: frontend
description: Arbor mobile app (Flutter) - frontend.
publish_to: "none"
version: 0.0.1+1

environment:
  sdk: ">=3.3.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  http: ^1.2.2
  supabase_flutter: ^2.8.0
  shared_preferences: ^2.2.3
  audioplayers: ^6.4.0
  flutter_webrtc: ^1.6.2+hotfix.1
  speech_to_text: ^7.4.0

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
