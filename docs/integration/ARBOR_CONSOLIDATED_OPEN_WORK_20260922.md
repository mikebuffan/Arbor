# Arbor — one consolidated open-work list

**Snapshot:** September 22, 2026 (PDT). **Owner:** integration coordination only. **Status:** documentation checkpoint, not a running worker, release authorization, or proof of production service. For changes, refresh exact PR heads, source code and receipts first. This list supersedes stale dates in the older assembly queue for owner-facing action planning; it does not supersede source code or each lane's authoritative handoff.

## Latest verified work — September 22 (supersedes older dated receipts below)

- [x] **Grove's owner/project discovery is implemented and CI-tested**, [draft #165](https://github.com/mikebuffan/Arbor/pull/165) at `c426683...`. Private API `GET /api/grove/ark/projects` returns only active explicitly granted and Firefly-owned project IDs. Flutter private auth → project gate selects one verified grant or requires explicit choice among several. Empty/revoked/failed responses cannot restore arbitrary local ARK context. [Exact-source backend + Flutter preflight 35790450802](https://github.com/mikebuffan/Arbor/actions/runs/35790450802) passed. **This is not proof any live owner grant exists yet.**
- [x] **Grove's signed private LM host transport exists, but is NOT wired into a public/client-facing chat route**, [draft #166](https://github.com/mikebuffan/Arbor/pull/166) at `da071d...`. It validates read-only owner/project/conversation context and bounded history, signs a fresh nonce/timestamp request for v0.3.5's independent Python receiver, rejects fake work receipts, and keeps all LM credentials server-only. [Pinned backend CI 35790928512](https://github.com/mikebuffan/Arbor/actions/runs/35790928512) passed. Actual private GPU inference, shared replay store, host→ARK Layer read, persistence, voice, consent and private API chat are **NOT finished**.
- [x] **Grove Projects shows only the locally selected private grant, not hard-coded queue/work claims**, [draft #167](https://github.com/mikebuffan/Arbor/pull/167) at `7c0fea8...`. Private Projects screen now tells the truth about selected local project vs actual read-only ARK status. Original Firefly visual placeholders stay separate. [Pinned full Grove backend + Flutter preflight 35791592394](https://github.com/mikebuffan/Arbor/actions/runs/35791592394) passed.
- [x] **Public-app #159** (backend + Android private-provider guards) and **ARK/Layer #160** (selected attachment bound to the requested conversation) passed exact-source CI in [35762754152](https://github.com/mikebuffan/Arbor/actions/runs/35762754152); both remain unmerged drafts, not hosted release proofs.
- [x] **Research #158** current lead dedupe head `d6dd049...` passed [its exact-head CI 35784260966](https://github.com/mikebuffan/Arbor/actions/runs/35784260966). Its own lane advanced to **research #163** at `b164d2f...` with [CI 35790795485](https://github.com/mikebuffan/Arbor/actions/runs/35790795485) green. #162/#164 are CI-only bridge PRs, not independent merge targets. No real sensitive-document ingestion or public finding release implied.
- [ ] **Highest-value next code integration:** in the owning lanes, reconcile the distinct Grove #167 and ARK/Layer #160 stacks; add a separately authorized private conversation route which validates Grove owner/grant, maps to a Firefly-owned project+conversation, calls `readArkLayerContext`, then invokes the signed private LM host transport and implements durable turn persistence/retry. DO NOT send Grove bearer to Firefly, use old `/api/chat`, expose raw client context, copy LM weights into public app or treat a model response as an ARK execution receipt.
- [ ] **Host/owner gates still real:** Vercel current-head deployment/denial proof, private owner invite+mapping+grants, live same-owner/cross-owner tests, phone Text↔Voice↔Text, real v0.3 adapter GPU evaluation, stable replay/rate limits, approved daytime art and actual Moss animation. Do not spend, merge or deploy without the appropriate permissions.

The sections below preserve the *earlier inspection timeline*. If an earlier line says #159/#160/#158 CI is pending or Grove #156 is the newest tip, use this latest verified checkpoint and [the updated v2 manifest](integration_manifest_v2.json) instead.

## North star and collision rules

One coherent **private Grove** shell with ARK durable user/project continuity, Arbor Layer correction/agency behavior, private Arbor LM and Text/Voice continuity; the **official public Arbor App is independent** and receives no private Grove/Firefly data; the **Epstein Evidence Project** has its own evidence pipeline, only entering the private environment through separately approved, scoped interfaces. The cross-lane index coordinates ownership and proof, not component code.

Never rebuild a tested component because it is unhosted; never infer deployment from CI, a phone screenshot from a debug APK, a running ARK worker from a persisted objective, or a model inference receipt from fake-model tests. Do not merge stacked parent PRs as independent releases. Do not copy Firefly keys to clients or public-app services, replay a manually executed SQL migration, commit model weights/user records/keys, silently create paid resources or switch on ARK execution.

## Actual immediate critical path — private Grove

- [x] Separate Grove Supabase project provisioned; private owner/mapping/project-grant tables applied and RLS verified. Both bridge tables applied through manual SQL Editor rather than migration ledger. Most recent observed rows: **0 owner grants, 0 account mappings, 0 project grants**. Two no-policy INFO notices are intentional client deny-all.
- [x] Dedicated private API read-broker and Grove-only Flutter route written and CI tested in [Grove #155](https://github.com/mikebuffan/Arbor/pull/155); private Grove branch stack preserved.
- [x] New `grove-private-api` Vercel project and production env configuration visibly confirmed by owner in Vercel UI. Connected Vercel project-list integration still reports Firefly only; its stale/incomplete inventory **does not negate the direct Vercel UI evidence**.
- [x] Branch [Grove deployment #156](https://github.com/mikebuffan/Arbor/pull/156) `deploy/grove-private-api-20260921` adds `crons: []`, defers unrelated legacy OpenAI SDK initialization until actual model use, and tests missing-credential behavior. **At exact head `8999a570cf20b8496f984f62a298a58ef67d7217`, [GitHub Actions 35696240828](https://github.com/mikebuffan/Arbor/actions/runs/35696240828) passed backend tests, TypeScript and Next.js backend build with `OPENAI_API_KEY` empty.** Do not add OpenAI credentials merely to make private read-only ARK route build.
- [ ] **Vercel host deployment:** GitHub status at that same head reported `Deployment rate limited — retry in 24 hours` for Grove as of 2026-09-22 06:44 UTC. This is a dated observation, not a continuously monitored or verified current limit. After capacity returns, deploy **current branch head**, not old failed missing-key deployment. No upgrade or billing change is authorized. Establish actual deployed commit and stable production hostname.
- [ ] **Live API acceptance:** test unauthenticated denial, non-Grove token denial, revoked-owner denial, wrong-user/project denial, wrong-host/other-route denial, and no Firefly cron/endpoint exposure. CI on fake contexts does not replace actual-host verification.
- [ ] **Owner identity/provider:** configure invite-only Grove OTP delivery; provision owner grant and explicit revocable owner-approved Grove-to-Firefly account/project mapping only after real authenticated IDs, no guessed UUIDs. Keep all privileged credentials server-only, no screenshots of values.
- [ ] **Manual SQL migration ledger:** reconcile the already-applied bridge SQL Editor DDL with migration record before any automated release replay; do not rerun DDL blindly.
- [ ] **Physical Android acceptance:** actual private Grove sign-in, house visual/window/sundial/clock, Moss persistence, ARK read for approved scope, fast A/P→B/Q and sign-out clearing, stale late-response rejection, unavailable state, restart, Text→Voice→Text same conversation and private/public app isolation. Source UI and debug APK are not signed-off device acceptance.
- [ ] **Controlled release:** exact branch promotion/rollback and release signing only after access/proof checks and owner approval. Mike only for an exact inaccessible local key/device/host/release action.

## Unblocked engineering work Arbor can do while the Vercel build limit holds

- [x] **First code-level Grove↔ARK/Layer seam review completed** at Grove #156 `8999a570...` and ARK/Layer #147 `ce70f722...`. Grove `readPrivateGroveArk` currently validates owner+bridge+explicit granted project, then returns `readArkProjectSnapshot` ONLY; it does **not** invoke `readArkLayerContext`, consume a conversation/mode/attachment, retrieve current-goal/corrections, or call the private LM. #147's `readArkLayerContext` separately checks owner/project/conversation/attachment before building bounded continuity/behavior context, and explicitly says `activeObjectiveHandoff:'not_resolved'`/`liveExecutionVerified:false`. Therefore: present Grove read API is a narrow status route, not proof that ARK+Layer+LM integration is complete. Preserve it and wire a distinct approved authenticated private conversation route in owning lanes; bind server-resolved Firefly identity and grant to conversation/project before #147 call, then sign scoped LM context server-side. No public app fallback. Further LM v0.3.5 receiver code/live inference review remains open. Refresh #149 collision manifest at promotion time (20-case offline validator has prior passing receipt).
- [x] **Synthetic security and continuity acceptance matrix inventoried** in [Grove × ARK/Layer matrix](GROVE_ARK_LAYER_ACCEPTANCE_MATRIX_20260922.md): inspected existing Grove broker (20 tests) and private-host isolation (5 tests), recorded 14 cross-lane cases as already-unit-tested vs pending end-to-end and actual owner-device checks. Account A/project P/conversation C vs B/Q, revoked owner/bridge, expiry, product JWT, in-flight switch, retry and model/task misclaims remain live acceptance gates. No implementation was duplicated and no synthetic test was reported as live.
- [x] **Grove UI/source review for current #156 stack:** inspected the approved-night house renderer, Living Window panel, shared preview selector, house clock, local Moss state/store/panel, and committed widget-test inventory. Existing source/test coverage includes preview→Return to Now without changing house clock, day stepping, door navigation, Moss restore after remount, and preserving unknown/damaged store data. **Remaining visual work is real:** the approved night image remains static, a matching daylight/window scene is not yet implemented; visual Moss position also stays painted on the couch even if local text/state moves Moss to rug. Do not claim complete Living Window/physical app from test names or these source reads. Fix only in Grove-owned branch after asset/composition review; owner phone acceptance remains open.
- [x] **LM host/ARK contract review:** read the actual private Library `Arbor_LM_Broker_Handoff_2026-09-21.md`: v0.3.5 has an isolated, synthetic/fake-model strict receiver and optional per-request signed-context HTTP endpoint; its Python receiver cannot independently re-prove owner identity because #147 read context omits owner ID. The *verified Grove host* must bind owner/project/conversation to the Firefly provider before a bounded signed envelope is sent. The currently deployed/read-only candidate Grove #156 endpoint ONLY returns an ARK snapshot and does not connect the LM. **Still open:** protected real GPU/Qwen3-0.6B + original v0.3 adapter inference, semantic identity/payment/tool tests, TLS/key rotation/rate limit and shared atomic replay nonce store for multi-instance production, verified host→Layer→LM actual request. No weights in public GitHub and no client-signed trust context.
- [x] **ARK × Layer source-scope audit and narrow fix drafted:** [#160](https://github.com/mikebuffan/Arbor/pull/160) now rejects a selected file whose conversation is absent or differs from the requested conversation *before* any DB read; two negative tests added and existing file fixtures bound. Reviewed work-order gate #147: pasted status cannot authorize execution or steal a running thread, and every disposition still sets grantsExecution:false. **#160 has pinned backend CI 35762754152**, and durable end-to-end ARK objective/restart/real host proof remains open. Do not enable execution.
- [x] **Public alpha source audit and narrow fix drafted:** existing #146 chat, session, export, deletion and cross-user route tests were inspected. Provider config previously blocked Firefly and ARK Preview but not newly created Grove; [#159](https://github.com/mikebuffan/Arbor/pull/159) now blocks Grove project ref and extends table-driven forbidden-provider test. **#159 has pinned backend+Flutter CI 35762754152**; separate public host, real model, consent/retention and two-user device/DB acceptance remain open. No new paid service or private data transfer.
- [x] **Research lane collision/source review:** #152 sandbox threat model/run.sh/Dockerfile inspected; #157 evidence-backed completion verification and #158 provenance-preserving lead dedupe already exist in research-owned stacked drafts. #157 exact head has green Arbor Integration CI 35700021998. **#158 current head has GitHub CI 35784260966 (green).** Independent benign third-party PDF manual fidelity and separate disposable database/worker permission remain pending. No real Epstein/EFTA PDF ingestion, public claims, or worker activation.
- [x] **Integration ledger and executable guard refreshed:** v2 manifest now tracks Grove #156 as sole tip (parent #155 retained), research #152 doc-only current head, and exact credential-free CI proof with stage TESTED not DEPLOYED. Fixed previously stale #155 bridge capability's inaccurate TESTED label (prior-code CI ≠ exact-head CI). Guard now requires a separate live-host receipt for DEPLOYED and owner evidence for ACCEPTED. Actual source checker and its 25-case committed suite executed in isolated JavaScript test harness: **25/25 passed**; freshly fetched main and 14 PR metadata checked: **PASS** on September 22. This is not a continuously refreshed watcher or a hosted Grove receipt. [Manifest](integration_manifest_v2.json) · [Guard](check_integration_v2.mjs) · [Tests](check_integration_v2.test.mjs).

## Human gates (only when actually reached)

**Danelle:** approved owner account invitation, actual-device observations, any new paid resource/production or public launch/release choice; no more manual “make a new project” chores based on stale state.

**Mike:** only when a signing key, GPU host, protected credential operation or local device action is genuinely inaccessible to connected tools, with an exact request. No “Mike has to do everything” default.

**Arbor:** source inspection, scoped reversible draft PRs/tests, collision analysis, truthful build and security receipts, documentation, and work remaining in *other unblocked lanes*. Not continuous background execution; each chat/tool action must actually run.

## Next return checkpoint

1. Refresh Vercel project UI/deployment receipt through supported access when possible; GitHub's old rate-limited status alone is not a fresh limit check.
2. Verify the latest exact Grove #156 branch/head and CI before a new production deployment; do not redeploy old failed `main` SHA.
3. Independently progress ARK × Layer and LM contract testing and the separate public/research review without touching the deployed Firefly or paid resources.
4. Once Grove host exists, test private owner/auth bridge, then physical Android and actual real-model continuity. Mark each level IMPLEMENTED, TESTED, DEPLOYED, ACCEPTED with separate evidence.

## Safe-work execution receipt, September 22 PDT

1. [x] Consolidate all five active product/research lanes and integration without merging their drafts.
2. [x] Refresh 14 tracked PRs and main in the dated v2 manifest; maintain accurate Grove #156 deployment tip/parent.
3. [x] Add exact-head credential-free build proof without pretending the Grove is live.
4. [x] Harden integration checker against CI-only claims of deployment/acceptance; 5 additional regression tests, **25/25 passed in isolated JS harness**. Refresh-check against GitHub PR heads/main passed.
5. [x] Inspect existing Grove auth/host unit-test inventory and create 14-row integration acceptance matrix; no duplicate component code.
6. [ ] Future engineering work in ARK/Layer, Grove UI, LM, public alpha, and research stays with their owning workstreams and is independent of Grove's Vercel quota; do not start unrelated branch edits from this integration lane.
7. [ ] Actual Vercel live deploy, invited owner/mapping, physical Android and real private LM require the specific runtime/owner gates described above.

**Human asks created by this receipt:** none. The current Vercel connector's project inventory is incomplete relative to owner-provided UI; do not infer the Grove project was deleted or deploy into Firefly.

## LM receiver-to-Grove source audit

The LM-side Library handoff is a description of **tested synthetic service components**, not a real-model/production receipt. The proposed host crossing has two distinct products and keys: a verified Grove client bearer must NEVER be forwarded as a Firefly credential, and a private server-only LM MAC key must NEVER be put in Flutter. Host resolves the Grove→Firefly grant, calls #147 only with the trusted mapped principal, and passes bounded signed per-turn state to LM; assistant text does not prove an ARK task finished. The current Grove status route remains deliberately narrower. This was a read-only architecture/contract audit, not a host implementation or credential action.

## Vercel-wait engineering receipt — September 22

- [x] Latest integration PR #149 at `b49d66ba...` had matching successful GitHub backend/Flutter/control checks. Executed the committed v2 checker/test-source against its manifest in an isolated JS harness: **25/25 passed** (not a substitute for GitHub CI). Inspected all 18 tracked PR head/base/state observations in two read-only batches and found no changed heads relative to the dated manifest snapshot.
- [x] Public app #159 and ARK/Layer #160 are preserved as **draft implemented source fixes with pending exact-head CI**, not deployed or approved.
- [x] Grove visual source/test inventory reviewed without rebuilding completed clock/Moss/sundial components or editing the deploy branch. Matching daytime/window artwork and moving Moss in the actual painting are not finished.
- [x] Existing research #157/#158 work found and preserved; no duplicate evidence engine built.
- [x] Added [browser-first Grove Vercel release and live denial acceptance card](GROVE_VERCEL_RELEASE_ACCEPTANCE_20260922.md) for latest correct branch, host isolation, owner invitation/grants, and no credential exposure. **Not a Vercel deployment.**
- [x] Exact-head PR #159/#160/#158 CI confirmed; private Grove host/live owner/device and model tests remain the actual gates.
